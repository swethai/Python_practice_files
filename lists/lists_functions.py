#!/usr/bin/python

aList = ['swetha', 123]
bList = ['swetha1', 123]

#camparing the lists
print "comparision of lists", cmp(aList, bList)
print "comparision of lists", cmp(bList, aList)

#no. of elements count in the lists

print "length of lists", len(aList)
print "length of lists", len(bList)

#max list

print "max element in the list", max(aList)

print "Min element in the list", min(aList)



